---
title: Rust (language)
aliases: [rustlang, empty]
---

Systems programming language. The other rust.md. No outgoing links.

Alias notes: `rustlang` is linked from a daily note to pin alias resolution.
Alias `empty` deliberately collides with empty.md's stem — the stem must win,
silently, with no ambiguity flagged.
